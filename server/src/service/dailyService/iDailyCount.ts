export default interface IDailyCount {
  // logo
  logoUrl: string;
  // 昵称
  nickname: string;
  // 赞(总数)
  upvote: number;
  // 被关注/粉丝(总数)
  follow: number;
  // 热度
  hot: number;
  // 约玩(总数)
  invite: number;
}
